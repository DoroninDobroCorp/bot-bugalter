# 🚫 Блокировка топиков - Краткая инструкция

## Проблема
Коды приходят во все ветки чата, включая "Отчеты", где они не нужны.

## Решение
Используйте команду `/block_topic` чтобы заблокировать конкретную ветку.

## Как использовать

### 1️⃣ Заблокировать ветку "Отчеты"
1. Зайдите в ветку **"Отчеты"**
2. Отправьте команду: `/block_topic`
3. Бот ответит: "🚫 Топик заблокирован! Бот больше не будет реагировать..."

✅ **Готово!** Теперь в ветке "Отчеты":
- Бот НЕ будет реагировать на фото
- Бот НЕ будет реагировать на "лайв"/"прематч"
- Бот НЕ будет генерировать коды

### 2️⃣ Разблокировать ветку
1. Зайдите в **заблокированную ветку**
2. Отправьте команду: `/unblock_topic`
3. Бот снова начнет работать в этой ветке

### 3️⃣ Проверить настройки
Отправьте `/show_ids_config` из любой ветки:
```
📋 Конфигурация для чата -1002969517449

✅ Чат включен для рассылки кодов
📌 Топик для кодов: 1
🚫 Заблокированные топики: 62, 45

🔹 Текущая ветка: 62 (ЗАБЛОКИРОВАН)
```

## Схема работы

```
Чат "Ставки"
│
├─ #General (ID: 1)          ← Коды ПРИХОДЯТ ✅
├─ #Ставки-Лайв (ID: 62)     ← Коды ПРИХОДЯТ ✅  
└─ #Отчеты (ID: 45)          ← Коды НЕ ПРИХОДЯТ 🚫 (заблокирован)
```

## Важно

- **Блокировка работает ТОЛЬКО для конкретной ветки**
- В других ветках бот продолжает работать нормально
- Чат остается активным
- Можно заблокировать сколько угодно веток
- `/set_ids_topic` работает независимо от блокировок

## Примеры

**Сценарий 1: Коды только в General**
1. `/set_ids_topic` в General
2. `/block_topic` в ветке "Отчеты"
3. Результат: Коды приходят в General, в "Отчеты" бот молчит

**Сценарий 2: Блокировка нескольких веток**
1. `/block_topic` в "Отчеты"
2. `/block_topic` в "Администрация"  
3. Результат: Эти 2 ветки игнорируются, остальные работают

## Команды

| Команда | Где вызывать | Что делает |
|---------|--------------|------------|
| `/block_topic` | Из ветки, которую блокируем | Блокирует текущую ветку |
| `/unblock_topic` | Из ветки, которую разблокируем | Разблокирует текущую ветку |
| `/show_ids_config` | Из любой ветки | Показывает все настройки |
| `/idchat` | Из любой ветки | Показывает ID чата и ветки |
